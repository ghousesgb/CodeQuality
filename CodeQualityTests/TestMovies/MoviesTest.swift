//
//  MoviesTest.swift
//  CodeQualityTests
//
//  Created by Ghouse Basha Shaik on 19/01/18.
//  Copyright © 2018 Ghouse Basha Shaik. All rights reserved.
//

import XCTest
@testable import CodeQuality

/*class MoviesTest: XCTestCase {
    // Unit Case for fetching Movies titles
    // Rule : Can't be empty and length not lesser than maxlength
    func test_MovieCount_isValid() {
        let storyboard = UIStoryboard(name: "Main", bundle: nil)
        let expectations = self.expectation(description: "API Call Waiting")
        let vc: ViewController = storyboard.instantiateViewController(withIdentifier: "ViewController") as! ViewController
       
        while (true) {
            if !vc.apiStatus {
                continue
            }
             let moviesVM = vc.moviesViewModel
            expectations.fulfill()
            XCTAssertEqual(moviesVM?.movies?.count, moviesVM?.numberOfItemsInSection(section: 0))
            break
        }
        waitForExpectations(timeout: 60.0, handler:nil)
        
    }
}*/
